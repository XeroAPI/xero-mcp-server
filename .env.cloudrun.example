# Xero MCP Server - Cloud Run Environment Configuration
# Copy this file to .env for local testing of HTTP server

# ===== XERO CONFIGURATION (Required) =====
# Get these from https://developer.xero.com/app/manage
# Create a Custom Connection: https://developer.xero.com/documentation/guides/oauth2/custom-connections/

XERO_CLIENT_ID=your_xero_client_id_here
XERO_CLIENT_SECRET=your_xero_client_secret_here

# Alternative: Use a bearer token instead of client credentials
# If set, this takes precedence over XERO_CLIENT_ID/SECRET
# XERO_CLIENT_BEARER_TOKEN=your_bearer_token_here


# ===== OAUTH 2.0 AUTHENTICATION (For Claude.ai Access) =====
# Enable OAuth 2.0 authentication to secure MCP server access

# Enable/disable authentication (set to "true" to enable)
AUTH_ENABLED=true

# OAuth 2.0 Client Credentials for Claude.ai
# These are the credentials that Claude.ai will use to authenticate to your MCP server
OAUTH_CLIENT_ID=claude-ai-client
OAUTH_CLIENT_SECRET=your-oauth-client-secret-change-me

# OAuth Token Secret (for signing JWT access tokens)
# Generate a strong random secret: openssl rand -base64 32
OAUTH_TOKEN_SECRET=your-oauth-token-secret-change-me

# Token expiry in seconds (default: 3600 = 1 hour)
TOKEN_EXPIRY=3600


# ===== SERVER CONFIGURATION =====

# Port (Cloud Run will set this automatically, default is 8080)
PORT=8080

# CORS - Allowed origins (comma-separated)
ALLOWED_ORIGINS=https://claude.ai,https://www.claude.ai

# Node environment
NODE_ENV=production


# ===== DEPLOYMENT CONFIGURATION =====
# These are used by deploy.sh script

# GCP Project ID
GCP_PROJECT_ID=your-gcp-project-id

# GCP Region
GCP_REGION=us-central1


# ===== LEGACY AUTHENTICATION (Optional) =====
# These are kept for backward compatibility but OAuth 2.0 is recommended

# JWT Secret for legacy JWT token mode (deprecated, use OAUTH_TOKEN_SECRET instead)
# JWT_SECRET=your-jwt-secret

# API Key for simple header-based auth (not recommended, use OAuth 2.0)
# API_KEY=your-api-key

# Basic auth for legacy /auth/token endpoint (deprecated)
# AUTH_USERNAME=admin
# AUTH_PASSWORD=your-password
